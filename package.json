{
	"name": "vdom",
	"version": "1.0.0",
	"license": "MIT",
	"type": "module",
	"scripts": {
		"build": "tsc && esbuild dist/vdom.js --minify --outfile=dist/vdom.min.js"
	},
	"devDependencies": {
		"esbuild": "^0.20.2",
		"typescript": "^5.4.0"
	}
}
